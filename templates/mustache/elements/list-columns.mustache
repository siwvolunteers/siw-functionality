<ul id="{{ element.id }}" class="{{ element.classes }}" data-columns="{{ columns }}" {{# list_style_type }} data-list-style-type="{{ . }}" {{/ list_style_type }}>
	{{# items }}
	<li>{{& . | wp_kses_post }}</li>
	{{/ items }}
</ul>
